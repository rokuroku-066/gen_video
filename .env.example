# Copy this file to .env and adjust values for your environment.
# GEMINI_API_KEY (or GOOGLE_API_KEY) is required only when calling the real Gemini/Veo APIs.
GEMINI_API_KEY=your_gemini_api_key

# Set to "1" to allow real API usage. Leave unset or "0" to stay offline-safe.
ENABLE_REAL_GENAI=0

# Set to "1" to force the built-in fake client for offline demos and tests.
USE_FAKE_GENAI=1
